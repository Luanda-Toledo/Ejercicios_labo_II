namespace claseUno
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
             * Imprimir texto en consola
                string cadena = "Hola Mundo";
                Console.WriteLine(cadena);
            */

            /*
             * Coversion
                object numero = 10;
                object numeroDos = 10;
                // Realizamos la conversion de un dato tipo object a int
                int resultado = (int)numero + (int)numeroDos;
                Console.WriteLine(resultado);
            */
        }
    }
}